# LangGraph API URL (backend / agent)
# Local: http://localhost:2024
# Production (Lambda): your Lambda Function URL from AWS
VITE_API_URL=http://localhost:2024

# Optional: API key for deployments that require X-Api-Key (Lambda usually does not)
# VITE_API_KEY=your_api_key

# Vercel API Configuration (for preview deployments)
VERCEL_API_TOKEN=your_token_here

# Optional: Team ID (if deploying to a team)
# VERCEL_TEAM_ID=team_xxxxx
